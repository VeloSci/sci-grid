import { describe, it, expect } from 'vitest';
import { VeloGridSolid } from './index';

describe('VeloGridSolid', () => {
  it('should be defined', () => {
    expect(VeloGridSolid).toBeDefined();
  });
});
