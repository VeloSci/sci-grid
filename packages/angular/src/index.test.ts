import { describe, it, expect } from 'vitest';
import { VeloGridAngular } from './index';

describe('VeloGridAngular', () => {
    it('should be defined', () => {
        expect(VeloGridAngular).toBeDefined();
    });
});
