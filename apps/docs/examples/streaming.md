# Real-time Streaming

The grid is optimized for high-frequency updates. In this example, values are updated randomly every 100ms.

Because SciGrid draws directly to canvas, re-renders are extremely cheap compared to DOM based grids.

<StreamingDemo />
