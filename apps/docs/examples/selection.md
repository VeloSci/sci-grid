# Selection Modes

SciGrid supports different selection modes via the SelectionManager API.
Use `CTRL` (Cmd) to select multiple cells, and `SHIFT` to select ranges.

Modes:
- `cell`: Default. Single or range of cells.
- `row`: Select full rows.
- `column`: Select full columns.

<SelectionDemo />
